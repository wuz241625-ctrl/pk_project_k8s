spec:
  template:
    metadata:
      labels:
        tenant: d7pay
    spec:
      containers:
        - name: admin
          envFrom:
            - configMapRef:
                name: d7pay-runtime-config
            - secretRef:
                name: d7pay-runtime-secret
